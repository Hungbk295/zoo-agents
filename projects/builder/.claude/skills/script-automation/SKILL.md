---
name: script-automation
description: Build automation scripts (shell, Python, JS). Trigger khi task cần automate workflow, write scripts, batch processing.
allowed-tools: Read, Write, Bash(*)
---

## Steps
1. Đọc RD để biết automation goal, inputs, outputs
2. Check existing scripts trước khi build mới
3. Write script → test → iterate
4. Document usage (how to run, dependencies, env vars)
5. Output: working script + usage docs trong RD
