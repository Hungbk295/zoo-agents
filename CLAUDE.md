# Zoo Workspace

Zoo is a multi-project orchestrator. Instructions auto-loaded from `.claude/rules/`.

- Task workflow: prep → run → review → wrap
- Task statuses: todo | scoped | doing | blocked | done
- Projects live under `projects/`
- Shared skills in `.claude/skills/`
- Slash commands: `/start`, `/end`, `/task`
